import styled from "styled-components";

const S = {
  Layout: styled.div`
    dislay: flex;
    align-items: center;
  `,
};

const M = {
  Layout: styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 150px;
    padding: 150px;
  `,
};

export { M, S };
