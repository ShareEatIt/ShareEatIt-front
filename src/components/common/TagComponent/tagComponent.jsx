/*import { ReactComponent as All } from "../../../assets/Home/All.svg";
    import { ReactComponent as Store } from "../../../../assets/Home/Store.svg";
    import { ReactComponent as Individual } from "../../../../assets/Home/Individual.svg";
    import { M } from "./tagComponent.style";

    const TagComponent = () => {
    const menuList = [
        { label: "전체", icon: <All /> },
        { label: "가게", icon: <Store /> },
        { label: "개인", icon: <Individual /> },
    ];
    useEffect(() => {
        clicked.label === "전체"
        ? setColorIndex(0)
        : clicked.label === "가게"
        ? setColorIndex(1)
        : setColorIndex(2);
    });
    const handleClick = (item) => {
        setClicked(item);
    };

    <M.SectionContainer>
        {menuList.map((item, index) => (
        <M.SectionClassificationButton
            key={index}
            onClick={() => handleClick(item)}
            $isActive={clicked.label === item.label}
        >
            {item.icon}
            {item.label}
        </M.SectionClassificationButton>
        ))}
    </M.SectionContainer>;
    };

    export default TagComponent;
*/
